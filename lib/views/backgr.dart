import 'package:flutter/material.dart';

class BackGr extends StatelessWidget {
  const BackGr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
